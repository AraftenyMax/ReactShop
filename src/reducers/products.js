import {ADD_TO_BUSKET, DELETE_FROM_BUSKET,
 UPDATE_ORDERED_COUNT, RECEIVE_PRODUCTS} from '../constants/ActionTypes.js';
import Products from '../Products.js';

const initialState = {
	orderedProductsId: []
}

const busket = (state = initialState, action) => {

}